import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Trabajo Final Hibridas',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
